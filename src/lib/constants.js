import {IMAGE_TYPES} from './MediaServices/constants';

export const DEFAULT_SIZE_FACTOR = 1;
export const DEFAULT_IMAGE_COMPRESSION = null;
export const DEFAULT_IMAGE_MIRROR = false;
export const DEFAULT_IMAGE_TYPE = IMAGE_TYPES.PNG;
